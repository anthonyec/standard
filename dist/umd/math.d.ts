/**
 * Convert an angle from degrees to radians.
 */
export declare function deg2rad(degrees: number): number;
/**
 * Convert an angle from radians to degrees.
 */
export declare function rad2deg(radians: number): number;
/**
 * Create sine waves using familiar parameters.
 */
export declare function sinWave(t: number, speed: number, amplitude: number, offset?: number): number;
/**
 * Create cosine waves using familiar parameters.
 */
export declare function cosWave(t: number, speed: number, amplitude: number, offset?: number): number;
